const styles = {
    container: {
        width: '100%',
        height: '66%',
        display: 'flex',
    },
};

const KeysSection = () => {
    return (
        <div style={styles.container}>
        </div>
    )
};


export default KeysSection;